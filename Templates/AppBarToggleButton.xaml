<!-- A button for toggling commands using app bar styling -->
<AppBarToggleButton x:Name="" 
                    Margin="0,0,0,0" 
					ClickMode="Release" 
					IsEnabled="True" 
					IsChecked="False" 
					IsCompact="False" 
					Label="Toggle">
</AppBarToggleButton>